void process1() {
    // Example without dedicated metadata mapping
    float set;
    float reset;
    float q = rsff_V1(set, reset);
}

void process2() {
    // Example with dedicated metadata mapping
    float set;
    float reset;
    float q = rsff_V2(set, reset);
}
